import { FormatarDataRelativaPipe } from './formatar-data-relativa-pipe';

describe('FormatarDataRelativaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarDataRelativaPipe();
    expect(pipe).toBeTruthy();
  });
});
